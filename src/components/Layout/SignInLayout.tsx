import { ISignInLayoutProps } from "@/interfaces/components.interface"

const SignInLayout = ({ children }: ISignInLayoutProps) => {
  return (
    <div>{children}</div>
  )
}

export default SignInLayout